// Admin-only global settings stub page.
export default function GlobalSettings() {
  return (
    <div className="rounded-lg border p-6">
      <h2 className="text-lg font-semibold">Global Settings</h2>
      <p className="mt-2 text-muted-foreground">
        System-wide configuration (admin only). This is a stub for future
        settings such as email templates, feature flags, and retention policies.
      </p>
    </div>
  )
}
